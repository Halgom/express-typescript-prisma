export abstract class Controller {
  // Place here any static method that you want all of your controllers to have access to
  // If a single controller requires a helper method, its better suited to place it in that specific controller
}
